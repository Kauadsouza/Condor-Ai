# CONDOR — instale com:  pip install -r requirements.txt

# Cérebro
openai>=1.55.0
python-dotenv>=1.0.0

# Servidor e interface
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pywebview>=5.0            # a janela própria, sem barra de navegador
pydantic>=2.6.0
pyyaml>=6.0

# Escuta sempre ligada (wake word local)
pvporcupine>=3.0.0
pvrecorder>=1.2.0

# Mãos no PC
psutil>=5.9.0             # estado da máquina
pyautogui>=0.9.54         # mouse e teclado
pyperclip>=1.8.2          # área de transferência
pillow>=10.0.0            # screenshot
pypdf>=4.0.0              # ler PDF
